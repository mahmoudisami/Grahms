package moteur;

import data.*;
import ihm.*;
import moteur.*;

public class GameProgress {
	
	// private long speed = 1000; à mettre dans gameScreen
	private static Clock clock;
	
	public void LaunchGameProgress (Clock clock, Grid grid) {
		if(clock.getDayCpt()==5) { //exemple

		}
		if(clock.getHour()=="7:00") {
			//APPEL fonction habitants parte au travail
		}
	}
}

